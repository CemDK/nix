#!/usr/bin/env bash

if [[ -f ./.ready-tmux ]]; then
    if [[ -x ./.ready-tmux ]]; then
        echo "Running .ready-tmux..."
        ./.ready-tmux
    else
        echo "Warning: .ready-tmux found but not executable — run: chmod +x .ready-tmux"
    fi
fi
